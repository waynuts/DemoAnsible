---
- name: docker and nginx
  hosts:
  remote_user: root

  tasks:
  - name: install docker
    yum:
      name: docker.io
      state: present
  - name: install docker compose
    yum:
      name: docker-compose
      state: present
  - name: start docker service
    service:
      name: docker
      state: started 